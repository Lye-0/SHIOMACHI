> 2026-09-20 の床下構造・固定ピン・補修セットの更新は [追加検証](pontoon-verification.md) を参照。以下の写真・件数は以前の検証時点の記録です。

# SHIOMACHI 検証結果

## 追補: 当たり判定・移動操作・補修表示（2026-09-20）

全景と接写の判定を画像に照合。部屋の扉・配管図・棚・引き手・機構・船・船台・水位目盛などのずれを修正した。輪郭が重要なものは多角形/楕円、単純な面は矩形とし、表示枠と実際に押せる形を一致させる。ユーザーから「正確でも狭すぎる」と指摘を受けたため、共通Hitで余白と38pxを基準とした最小外接寸法を確保し、黄色い枠も一緒に広げた。子の写真は元の大きさと位置を保つ。取得物は透明余白を除いた範囲を基準に、操作用の余裕を追加する。

解錠済みの鍵、取得後の補修板の棚、清掃済み吸込み口、補修済みの傷、排水済みの栓、抜いたピン、写し取り済みの船台など、役目を終えた判定を撤去。解錠済みの旧接写から再操作しても「途中で折れた鍵が必要だ」と言わない。引き出しは解錠後も引き手で開閉できる。

移動・視点切替・再操作の文字ボタンは写真の外のActionBarへ集約し、48px以上の高さを確保。船内/船底、乗船、帰路、陸側、接写から戻る操作の位置を統一した。水路の進行は水面の大きな透明枠から文字ボタンへ変更。演出内の複製描画は操作欄にボタンを登録しない。

マップは訪問済みの場所をマウスまたはEnter/Spaceで選択して移動できる。未訪問・水没した整備通路・高さが足りない観測室・航行中/終了後は移動させない。移動時は拡大と向きを解除し、所持品/仕掛け/記録は保持。UIとengineは同じ制約を参照する。

船体の外側の補修具はアイテム画像の貼付けをやめ、低/中/高水位の専用画像へ変更。船板に薄く密着させ、割れ目を覆い、色と遠近感を写真へ合わせた。水没時には補修具を空中に表示しない。採用画像と内蔵生成のプロンプトは `../assets/boat-repair-generation.json`。

検証: 全143 fixtureを最終版で描画してImage.decodeと画面外/到達可能判定を検査、画面外0・到達不能0。結果は `target-audit.json`。全景/接写/水位/取得途中/完了状態を撮影し、変更対象は拡大して目視。製品版でマップのクリック/Enter移動、判定表示、水位目盛の選択と復帰を確認。開いた引き出しの鍵判定0・解錠済み接写の鍵操作0、引き手の開閉を実操作。390px幅でも水位目盛の選択と48pxの移動ボタンを確認。41テスト、TypeScript検査、製品ビルド成功。

UI Evolveの操作と状態の方針に従い、「対象との対応」と「押しやすい余裕」の両方を確認した。Reactの確認では、操作欄のportal・ResizeObserverの解除・キーボード操作・演出内の非操作描画を点検した。テスト件数は画像の完全性を保証するものではなく、今回の照合結果と併せて扱う。

## 追補: 進行に合わせたヒント（2026-09-20）

接写中の対象を無条件に優先する固定ヒントを廃止し、現在の進行・所持品・水位・記録から次の未完了作業を選ぶ。工具箱を開けた後の取得、鍵の抜取り/組合せ/使用、引き出しの取り残し、補修板の取付け/ボルト/排水、船の片側補修/水汲み、船灯の装着/清掃/点灯を分けた。低水位作業が残る場合は水門の閉鎖と排水を先に案内する。観測・測線・測深・水門・出航・航行中の現在位置・終了も区別する。

見ている場面が解決済みでも次の作業を表示する。紙片や測線は保存された記録も参照する。工具箱の途中配置に初期配置専用の手順を出さず、現在の未完了作業を案内する。HintPanelはヒントの進行IDが変わると最初の段階へ戻り、新しい対象の詳細解答を突然表示しない。セーブ形式やゲームの進行条件は変更していない。

回帰テスト15件を追加し、既存と合わせ35件成功。製品ビルド成功。Playwright MCPで製品版のメニュー→手がかり→段階表示と終端ボタンを確認。保存領域を使わない開発画面で、進行更新時の段階リセットと工具取得後の文面を実際のHintPanelで確認し、スクリーンショットを目視した。ui-evolveの状態と文言の既存方針を適用し、今回固有の分岐はテストへ保存した。

## 追補: 事務室・整備工房の入口の重複（2026-09-20）

前回は出口を操作できることを確認したが、回転前後の写真に別々の出口がある構造矛盾を見落とした。両室を「連絡桟橋側に入口が一つ、入室後は奥の机・作業台を見る、振り返ると入口を見る」に統一した。事務室の奥の扉、工房の左の開口を壁に変更。開いた引き出しの差分にも同じ壁を適用した。事務室の入口側の余分な机と椅子を除き、緑の六枚ガラスの扉に統一。工房は両開きの木製扉に合わせた。桟橋側から覗く室内も更新。

室内奥の旧出口のクリック領域を撤去し、常設ボタンを「入口を振り返る」に変更。入口側だけに桟橋へ出る操作を置く。左右操作と同じface遷移を使用し、既存セーブの進行データは変更しない。画像は一律に全景の拡大で代用せず、部屋の向きごとの専用画像を修正した。

Playwright MCPで桟橋→両室→入口を振り返る→扉で桟橋へ戻る、文字ボタンでの退室を実操作。製品ビルドでも左右の振り返り・往復を確認した。桟橋、両室の前後、引き出し開閉、工具箱の解答前後を撮影して目視比較した。確認中に見つけた解答後の工具表示の箱外へのはみ出しも内側へ修正。20テストと製品ビルド成功。

今回の確認はこの2室と入口・関連状態の修正に対するもの。過去の全場面チェックの件数を空間整合性の保証とはしない。ui-evolveのaction-relationshipsを参照し、向きを変える操作と退室操作を区別した。今回の具体的な空間条件は本追補へ保存し、一般UI知見への追加は行わない。

比較画像: [room-layout-comparison.jpg](room-layout-comparison.jpg)。採用6枚と内蔵image_genのプロンプト: [room-layout-generation.json](../assets/room-layout-generation.json)。

## 追補: 水門の遠景と接写（2026-09-20）

遠景に接写の金具だけを貼る方法では鮮明さ・取付け感が合わず、接写も門の全体形を失っていた。内蔵image_genで石のアーチ・両開きの扉・X補強材・取付け済みの水位窓と閂を含む専用接写を再生成。低/中/高水位と開門後を同じカメラで作り、GateViewを全景へ縮小投影する。部品の鮮明さだけが残らないよう扉全体に同じ距離表現を適用する。

7状態（全景の低・中・高・開門、接写の水圧差・同水位・開門）を撮影・目視し、製品版の全景→接写も対比較。低/中での圧力による開門不可、高での開門・再閉鎖・全景への復帰を実操作確認。水位窓の位置を新しい枠へ合わせ、開いた葉にも水位を表示。コンソールエラー0、20テストとビルド成功。素材102枚、欠落/未登録/未使用0。旧水門写真・旧開門全景と不採用候補は整理した。

比較画像: [gate-comparison.jpg](gate-comparison.jpg)。画像・プロンプト: `../assets/gate-generation.json`。旧資料中の水門画像はこの追補で更新した。

## 追補: 記録帳への自動記録（2026-09-20）

ユーザーの希望により手動記録を廃止。図面完成、柱と渡り板、配管図、水路の梁、観測窓、測量図、型の写し、浮いた船の水線を観察時に自動記録する。観察中の位置・測線変更も更新し、未完成の紙片から完成図を先に見せない。物理的な写し取りや道具を使う操作は引き続きプレイヤーが行う。

製品版で柱と渡り板・配管図を開くだけで記録帳に追加され、記録ボタン0件、再読込み後も保持されることを実操作確認。紙片完成、全8記録対象、観察中の更新、他所へ移動後の保持を含む20テストと製品ビルド成功。従来の手動記録の操作手順・写真中の記録ボタンは本追補で置き換えた。

## 追補: 船溜まりの左右操作（2026-09-20）

先の画像比較では「左を見る」で同一写真が拡大する意味の不一致を見落とした。船溜まりを一つの全景に統合し、左右矢印と左右キーによる切替を撤去。船・船灯・水門・船台・水位目盛を直接選び、乗船は独立した文字ボタンにした。出航演出も同じ全景から始める。

製品版で左右矢印0件、左右キー入力前後のtransformがnone、5対象の開閉と桟橋への帰路、他の部屋の矢印2件を確認。低・中・高水位の修理済み/浸水状態を目視し、沈んだ船灯の選択範囲も追従することを確認。旧face=1のセーブは進行と接写を保って全景へ正規化する。18件のテストと製品ビルド成功。現行全景は [dock-navigation-fixed.png](dock-navigation-fixed.png)。下記134状態・63選択は前回修正時の記録で、船溜まりの二方向表示は本追補で更新した。

ui-evolve の action-relationships の既存方針に従い、操作の見た目と役割・結果を比較した。既存知見の適用漏れのため一般規則の追加は行っていない。

2026-09-20。全場面整合性修正後の Chromium / Playwright MCP による結果。

## 結果

- 全8エリア・16方向の63選択を実際に操作し、選択前後を撮影・目視比較。選択後の画像読込失敗0。
- 全景、全接写、水位、取得途中、開閉、補修途中、船灯、航路など134状態を撮影。修正箇所は原寸でも確認。
- 新規状態から状態注入なしで全課題を通し、終章到達と保存再開を確認。
- `pnpm test`: 17件成功。支持具・水位・修理・航路・保存・記録帳の測線保存・装着後の船灯清掃を含む。
- `pnpm build`: TypeScript検査と製品ビルド成功。
- `pnpm check:assets`: 採用100枚、34.18MiB。欠落・未登録・不正寸法・未使用0。
- 製品版で画像100枚をブラウザの Image.decode に通し、失敗0。検証場面セレクター0件。製品JSにVisualLabと検証用fixtureがないことも確認。
- 製品版でも最初の窓、三本の留め金、独立した操作場所表示ボタン、桟橋と船溜まりの往復を実操作。
- 390×844のマップ・メニュー、844×390のメニューを目視。横方向の超過なし。Escapeで閉じ、マップボタンへフォーカスが戻る。

## 証拠の読み方

[点検表と修正内容](consistency-audit.md)、[状態名と操作結果](consistency-results.json)を参照。

`consistency/pair-states-00-0.jpg`〜`pair-states-33-0.jpg` は134状態の一覧。`consistency-results.json` の fixture と quadrant（左上0、右上1、左下2、右下3）を対応させる。

`consistency/pair-audit-*.jpg` と `pair-waiting-closed-*.jpg` は16方向からの63選択。各組の before が全景、番号付きがその場所から選んだ結果。これは読込検査だけでなく同一物・同一状態の目視比較に使用した。

`consistency/flow-*.jpg` は通常プレイの途中状態、喫水のドラッグ計測、記録帳、船灯、出航、浅瀬・梁の反応、終章、狭幅UIの確認。出航演出に含まれる画像は dock-high、dock-high-open、boat-kit、boat-lamp、sea-night と航路標で、桟橋写真を含まない。

従来の50枚の一覧だけでは、全景と接写の連続性を十分に確認できていなかった。旧QA写真は今回の比較画像に置き換えた。最終状態の一括撮影はMCPの300秒応答上限を超えたが、134枚の保存と最終fixture番号133への到達を確認し、選択操作は4つに分けて再実行した。

## 保存と素材

既存の保存データを消さず、新規の localhost:5198 を通し確認に使用。旧形式の記録帳も読込み可能。検証用fixture画面は保存領域を使用しない。

公開素材は scenes / closeups / items / mechanisms に整理。今回不要になった旧接写5枚を除去し、不採用・置き換え済みの原本28枚も削除した。採用原本100枚を保持。削除履歴は `../assets/discarded-sources.json`、内蔵 image_gen による修正プロンプト・採否は `../assets/consistency-generation.json`。

## 検証の境界

これは解法を知った作者による確認であり、初見の人による試遊ではない。134状態は列挙した表示状態の網羅で、すべてのフラグの直積や全ブラウザ・実機タッチ操作を総当たりしたという意味ではない。目標150〜240分、初見難易度、音響の聴感評価は未計測。
